{

  "api-key": {
    "name": "cm-api-key",
    "description": "平台发行的 API_KEY",
    "in": "header",
    "type": "string",
    "required": true
  },

  "credentials": {
    "name": "credentials",
    "in": "body",
    "description": "凭证信息",
    "required": true,
    "schema": {
      "$ref": "refs.json#/definitions/credentials"
    }
  },

  "id": {
    "name": "id",
    "in": "path",
    "description": "实体 ID",
    "type": "string",
    "required": true
  }

}
